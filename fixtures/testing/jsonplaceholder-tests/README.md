# testing/jsonplaceholder-tests

Requests with Assert-tab rows and Tests-tab scripts that pass against `jsonplaceholder.typicode.com`, so the
response Tests tab shows green results ("Tests (2), Passed: 2" and "Assertions (3), Passed: 3").
