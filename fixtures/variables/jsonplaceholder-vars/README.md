# variables/jsonplaceholder-vars

A request whose URL and headers are built from three kinds of variables: an environment variable
(`baseUrl`), a collection variable (`apiVersion`) and a request variable (`userId`, on the Vars tab).
