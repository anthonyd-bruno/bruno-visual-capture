# authoring/blog-api

A small "Blog API" collection over `jsonplaceholder.typicode.com` (posts and users) for the authoring,
code-generation and response workflows: list posts, get one user, create a post. Requests are edited live
(new request, headers, query params, clones, folders) on a fresh copy every run.
