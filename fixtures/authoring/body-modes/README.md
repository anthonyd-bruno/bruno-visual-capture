# authoring/body-modes

One POST request per body type — JSON, XML, Form URL Encoded, Multipart Form and plain text — all against
`jsonplaceholder.typicode.com/posts` (which echoes what it receives), so the Body tab has real content in every mode.
