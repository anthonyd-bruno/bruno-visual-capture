info:
  name: Form URL encoded
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/posts"
  body:
    type: form-urlencoded
    data:
      - name: title
        value: Hello from Bruno
      - name: body
        value: Sent as a form
      - name: userId
        value: "1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
