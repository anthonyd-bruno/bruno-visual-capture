info:
  name: Get user posts
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/users/1/posts"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
