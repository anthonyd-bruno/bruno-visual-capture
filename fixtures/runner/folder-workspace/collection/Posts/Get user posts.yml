info:
  name: Get user posts
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/posts?userId=1"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
