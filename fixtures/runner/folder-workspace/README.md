# runner/folder-workspace

A "Folders Demo" collection organised into folders (`Users/`, `Posts/`) plus one root request, all against
`jsonplaceholder.typicode.com`. The `Users` folder carries folder-level headers and inherits auth, so folder settings,
folder runs and sidebar search have something to show.
