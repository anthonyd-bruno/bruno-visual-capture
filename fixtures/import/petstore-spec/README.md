# import/petstore-spec

Only an OpenAPI 3 document (`spec/openapi.yaml`, the same one the OpenAPI Sync fixture uses) and **no collection**, so
the workspace starts empty and the import workflow creates the collection from the spec.
