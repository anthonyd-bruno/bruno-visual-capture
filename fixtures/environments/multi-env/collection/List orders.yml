info:
  name: List orders
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/todos"
  headers:
    - name: Accept
      value: application/json

  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
