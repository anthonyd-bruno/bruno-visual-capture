# scripting/jsonplaceholder-scripts

A request with a pre-request script (sets a variable and a header from it) and a post-response script
(stores a value from the response), plus a test that reads the variables — everything the Script and Tests
tabs need to show meaningful content.
