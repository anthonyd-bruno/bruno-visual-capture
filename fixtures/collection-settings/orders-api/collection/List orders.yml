info:
  name: List orders
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/todos"
  params:
    - name: _limit
      value: "{{pageSize}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
