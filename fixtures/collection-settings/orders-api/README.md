# collection-settings/orders-api

An "Orders API" collection with every collection-level setting populated — headers, variables, bearer auth and
a pre-request script — so the Collection Settings tabs have content to show. Requests inherit the auth and
use the collection variables.
